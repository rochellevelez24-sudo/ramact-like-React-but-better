# Ramact like React but better

## Ramact is an JS libary that is better than React!

# Example: 

```javascript
import { Elements } from "ramact/elements"

Elements.create("Text" {
   ["Properties"]: {
      ["Size"]: "10"
   }
})
